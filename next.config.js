/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Comments related to webpack or pageExtensions can also be removed for clarity
  // if the webpack config is definitely not needed.
};

module.exports = nextConfig;
